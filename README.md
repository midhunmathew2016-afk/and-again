# And Again — Basic Page

This folder contains a minimal starter page.

Open `index.html` in a browser to view the page:

```bash
# macOS / Linux
open index.html

# Windows (PowerShell)
Start-Process index.html
```
